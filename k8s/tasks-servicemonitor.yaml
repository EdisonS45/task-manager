apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: tasks-servicemonitor
  namespace: monitoring
  labels:
    release: monitoring
spec:
  namespaceSelector:
    any: true
  selector:
    matchLabels:
      app: tasks
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
